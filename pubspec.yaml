name: flutter_application_tubes
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  mqtt_client: ^10.10.0
  http: ^1.1.0
  google_fonts: ^6.1.0
  fl_chart: ^1.0.0
  rename: ^3.1.0
  flutter_launcher_icons: ^0.14.4
   

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 5.0.0

flutter_launcher_icons:
  android: "launcher_icon"
  image_path: "assets/icons/icon.png"

flutter:
  uses-material-design: true
